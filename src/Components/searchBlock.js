import React from 'react';

const SearchBlock = () => {
    const searchBlockText = 'What are you looking for?'
    return <input placeholder={searchBlockText}></input>;
}

export default SearchBlock;